{
    "version": 2,
    "builds": [
        {
            "src": "clientapi/package.json",
            "use": "@vercel/static-build"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/clientapi/$1"
        }
    ]
}